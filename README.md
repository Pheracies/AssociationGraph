# AssociationGraph
A small C++20 template library for building and exploring graphs of arbitrary value types. Each vertex stores a value and its adjacency list; graphs can contain other graphs as values, with automatic back-pointers to the contained graph detected at compile time.
